site:
  title: 刘旭Python
  description: 跟着旭哥学Python.
  keywords: 刘旭 liuxu python
  theme: default
  language: zh-CN
  # set the /<rootPath> pathname under which your site is served
  # for GitHub pages deployment without a custom domain, it is often '/<projectName>'
  # root-path: /gitsite
  # set cdn path:
  cdn-path: https://cdn.jsdelivr.net/npm
  navigation:
    - title: Pyhton教程
      uri: /books/python/index.html
  contact:
    name: 刘旭
  books:
    index-marker: true
  search:
    # none = no search
    # browser = flex search in browser
    # google = google site search
    type: browser
  # control styles:
  styles:
    # show 'Edit in Git':
    display-git-link: false
    # any custom styles used in theme:
    # code-font: Monaco
  integration:
  footer: |
          <div>
            由GitSite提供支持。版权所有&copy;<a href="/">刘旭Python</a> 
            - <a href="https://space.bilibili.com/433467371" target="_blank">B站</a>
            - <a href="https://github.com/liuxupython" target="_blank">GitHub</a>
          </div>


build:
  # copy special files under root dir (ignore if not found):
  copy:
    - favicon.ico
    - robots.txt
    - ads.txt

# you can add any items:
